# Template de Prompt para Criar uma Rota de API

Crie a rota `PATCH /workout-plans/{id(uuid)}/days/{id(uuid)}/sessions/:id(uuid)`

## Descrição

Ela atualiza uma sessão de treino epecífica


## Autenticação

- Rota protegida.
- O usuário precisa ser dono do plano de treino recebido.

## Request

[Descreva os dados de entrada,por exemplo]

```ts
interface Body {
  completedAt: string; // ISO STRING (ex.: '2026-03-05T20:34:37.004Z')
}
```

```ts
interface Params {}
```

```ts
interface Query {}
```

## Response

[Descreva o retorno esperado e o status code, por exemplo:]

```ts
interface StatusCode200 {
  id: string;
  completedAt: string // ISO STRING DATE (ex.: '2026-03-05T20:34:37.004Z')
  starteddAt: string // ISO STRING DATE (ex.: '2026-03-05T20:34:37.004Z')
}
```
